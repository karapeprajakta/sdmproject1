create table person (
    id integer primary key auto_increment,
    firstName varchar(100),
    lastName varchar(100),
    age integer
);

create table car (
    id integer primary key auto_increment,
    title varchar(100),
    company varchar(100),
    price integer
);